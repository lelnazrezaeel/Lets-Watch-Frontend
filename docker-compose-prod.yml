version: "3.8"

services:
  frontend:
    container_name: tracer-frontend-prod
    image: tracer-frontend-prod
    build: 
      context: .
      dockerfile: Dockerfile
    env_file:
      - config/env/.env
    prots:
      - "8080:8080"